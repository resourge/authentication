export const STORAGE_TOKEN_KEY = '_AC_';
export const STORAGE_REFRESH_TOKEN_KEY = '_ACR_';

export const StorageKeys = [
	STORAGE_TOKEN_KEY,
	STORAGE_REFRESH_TOKEN_KEY
];

export const IS_DEV = process.env.NODE_ENV === 'development';
